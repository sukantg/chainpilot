{
  "installCommand": "npm install --prefix .. && npm install",
  "buildCommand": "npm run build",
  "framework": "nextjs",
  "functions": {
    "src/app/api/mcp/route.ts": {
      "maxDuration": 60
    }
  }
}
